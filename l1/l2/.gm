{…}:
(
	∈ bracket,
),

{…|…}:
(
	∈ bracket,
),

∅: = {},

Ps(∀A: ∈ set): = {S|∀S: ⊆ A},

Range(∀f: ∈ mapping): = {f(x)|∀x: ∈ f.D}

∪:
(
	∈ symbunc,

	∀τ: ∈ set, ; set of set
	∪(τ): = {x| ∃C: ∈ τ, x ∈ C},

	∀A: ∈ ◉, ∀B: ∈ ◉,
	∀(A ∈ set, B ∈ set) ⇒ (A ∪ B): = ∪({A, B})
),

∩:
(
	∈ symbunc,

	∀τ: ∈ set, ; set of set
	∩(τ): = {x| ∀C: ∈ τ, x ∈ C},

	∀A: ∈ ◉, ∀B: ∈ ◉,
	∀(A ∈ set, B ∈ set) ⇒ (A ∩ B): = ∩({A, B})
),

×:
(
	∈ symbol,
	∀A: ∈ set, ∀B: ∈ set, (A × B): = {(x, y)| ∀x: ∈ A, ∀y: ∈ B}
)