{…}:
(
	∈ bracket,
)

{…|…}:
(
	∈ bracket,
)

Ps:
(
	∈ function,
	∀A: ∈ set, Ps(A): = {S|∀S: ⊆ A},
),

∪:
(
	∈ symbunc,

	∀τ: ∈ set, ; set of set
	∪(τ): = {x| ∃C: ∈ τ, x ∈ C},

	∀A: ∈ ◉, ∀B: ∈ ◉,
	∀(A ∈ set, B ∈ set) ⇒ (A ∪ B): = ∪({A, B})
)

∩:
(
	∈ symbunc,

	∀τ: ∈ set, ; set of set
	∩(τ): = {x| ∀C: ∈ τ, x ∈ C},

	∀A: ∈ ◉, ∀B: ∈ ◉,
	∀(A ∈ set, B ∈ set) ⇒ (A ∩ B): = ∩({A, B})
),

×:
(
	∈ symbol,
	∀A: ∈ set, ∀B: ∈ set, (A × B): = {(x, y)| ∀x: ∈ A, ∀y: ∈ B}
)