; Theorem: start
	@giacomo.h.group_theory, ; location
	∀C: ∈ coset,
	∃~: ∈ equivalence_relator, &~&.D = C.G,
	∀a: ∈ C.G, a ~ b ⇔ C.G.e (C.G.* a)^-1 C.G.* b ∈ C.G ■
; Theorem: end

; ! Theorem: start
	@giacomo.h.group_theory,
	G: ∈ group,
	∀H: %subgroup% G, ∀K: %subgroup% G,
	inter:
	(
		∈ group,

		D: = H ∩ K,
		*: = &G.*&,
		e: = G.e
	),
	inter %subgroup% G ■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀m: ∈ N \ {0}, Z/(m × Z) = {{x + m × z|∀z: ∈ Z}|∀x: ∈ Z}
	∃𝒴:
	(
		∈ group,
		D: = {y| ∀y: ∈ C, y ^ m = 1},
		*: = &+&,
		e: = 1
	),
	Z/(m × Z) ≌ 𝒴 ■
; Theorem: end

s"Lagrange Theorem":
(
	@giacomo.h.group_theory,
	G: ∈ group, H: %subgroup% G,
	|G| ∈ N ⇒ |G|/|H| ∈ Z ■
),
; Proof: start
	; Lemma: start
		@giacomo.h.group_theory,
		G: ∈ group, H: %subgroup% G, *: = &G.*&
		∀x: ∈ G, |H| = |x * H| ■
	; Lemma: end
	; Proof: start
		@giacomo.h.group_theory,
		G: ∈ group, H: %subgroup% G, *: = &G.*&
		∀x: ∈ G,
		∃φ[x]:
		(
			∈ mapping,
			D: = H, C: = x × H,
			φ[x](∀h: ∈ H): = x * h
		),
		(
			(
				∀h: ∈ H, ∀h': ∈ H,
				φ[x](h) = φ[x](h') ⇒ x * h = x * h' ⇒ h = h'
			)
			⇒ φ[x] ∈ injection,
			(
				∀y: ∈ φ[x].C,
				∃h: ∈ H, y = x * h, φ[x](h) = y
			)
			⇒ φ[x] ∈ surjection
		)
		⇒ φ[x] ∈ bijection
		⇒ |φ[x].C| = |φ[x]|
		⇒ |H| = |x * H| ■
	; Proof: end
	@giacomo.h.group_theory,
	G: ∈ group, H: %subgroup% G, *: = &G.*&
	(∀x: ∈ G, G.e ∈ H ⇒ x ∈ x * H) ⇒ !(x * H = ∅),
	|G| ∈ N ⇒
	(
		∃m: (∈ N, ≤ |G|),
		|G| = |∪({x * H|∀x: ∈ G})|
		= |∪({x[n] * H|∀x[∀n: (∈ N, ≤ m)]: ∈ G})|
		= +[∀n: (∈ N, ≤ m)](|x[n] * H|)
		= |H| × m
	)■
; Proof: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ cyclic_group,
	∀H: %subgroup% G,
	H ∈ cyclic_group ■
; Theorem: end
; Proof: start
	; @giacomo.h.group_theory,
	; ∀G: ∈ cyclic_group,
	; ∀H: %subgroup% G,
	; ∀(
	; 	H = {G.e} ⇒ H ∈ cyclic_group,
	; 	!(H = {G.e}) ⇒
	; 	(
	; 		∃a: ∈ H, !(a = G.e),
	; 		ex(a): = {G.e (G.* a)^n|∀n: ∈ Z}
	; 		(
	; 			ex(a) ⊆ H, ; H is a group
				
	; 		)
	; 	)
	; )
; Proof: end

"The converse of \"Lagrange\" when G is cyclic"
; Theorem: start
	@giacomo.h.group_theory,
	G: ∈ group, 
	|G| ∈ N ⇒ (∀d: ∈ N m/d ∈ Z ⇒ (∃H: %subgroup% G, |H| = d))■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀m: (∈ N, > 1), ∀n: (∈ N, > 1),
	Z/(m × Z) × Z/(n × Z) ≌ Z/((m × n) × Z) ⇔ gcd(m, n) = 1■
; Theorem: end

s"Fundamental Theorem of Finite generated abelian group":
(
	@giacomo.h.group_theory,
	∀G: ∈ finitely_generated_group,
	G ∈ abelian_group ⇒ (∃m: ∈ N, ∃T: ∈ abelian_group, |T| ∈ N, G ≌ Z^m × T)■
),
; Proof

s"Cayley's Theorem":
(
	@giacomo.h.group_theory,
	∀G: ∈ group, ∃T: %subgroup% sym(G), G ≌ T■
),
; similarly
; Theorem: strat
	@giacomo.h.group_theory,
	∀G: ∈ group, ∃T: %subgroup% S[|G|], G ≌ T■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀μ: ∈ action, G: = μ.G, stab[μ](∀s: ∈ μ.S) %subgroup% G■
; Theorem: end

s"Orbit-Stabilizer Theorem":
(
	@giacomo.h.group_theory,
	∀μ: ∈ action, ∀s: ∈ μ.S,
	|orb[μ](s)| ∈ N ⇒ |G / stab[μ](s)| = |orb[μ](x)|
	; with the help of "Lagrange Theorem", we get
	∀(|orb[μ](s)| ∈ N, |μ.G| ∈ N) ⇒ |G| = |stab[μ](s)| × |orb[μ](x)|
	■
),

s"Sylow's First Theorem":
(
	@giacomo.h.group_theory,
	∀G: ∈ group,
	|G| ∈ N ⇒
	(
		∀p: ∈ prime_number, ∀n: ∈ N, m: = |G|/(p^n),
		∀(m ∈ N, gcd(m, p) = 1) ⇒
		(
			∃H: %subgroup% G, |H| = p^n
		)
	)■
),

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group, center(G) %subgroup% G■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀p: ∈ prime_number, ∀n: ∈ N,
	|G| = p^n ⇒ !(center(G) = {G.e})■
; Theorem: end

; * About S[n], A[n], D[n]
; Theorem: start
	@giacomo.h.group_theory,
	∀n: ∈ N,
	S[n] = gp({c(a, b)|∀(a, b): ∈ N^2, a ≤ n, b ≤ n}),
	A[n] = gp({c(x, y, z)|∀(x, y, z): ∈ N^3, x ≤ n, y ≤ n, z ≤ n}),
	σ: = D[n].σ, τ: = D[n].τ,
	D[n] = gp({σ∘τ, τ}) ⇐ σ∘τ ∘ τ = σ,
	order[D[n]](σ) = n,
	order[D[n]](τ) = 2,
	τ∘σ = σ^-1 ∘ τ
	■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group,
	G ∈ abelian_group ⇔ (∀H: %subgroup% G, H ∈ normal(G))■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀No: ∈ normal(G),
	∃H: ∈ group, H = G / No, 
	H.*
	(
		∈ symbol, ∀(x, y): ∈ (G / No)^2,
		(x G.* No) * (y G.* No) = (x * y) G.* No
	)■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀No: ∈ normal(G),
	∀H: %subgroup% G, No ⊆ H ⇒ No ∈ normal(H)■
; Theorem: end

s"First Isomorphism Theorem": ; !
(
	∀f: ∈ homomorphism, ∃F: ∈ isomorphism,
	F.D = f.D/ker(f), F.C = Range(f),
	∀x: ∈ f.D, F(x G.* ker(f)) = f(x),
	; what's more
	|G| = |ker(f)| × |Range(f)|
),

s"Second Isomorphism Theorem":
(
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀H: %subgroup% G, ∀No: ∈ normal(G),
	(H ∩ No) ∈ normal(G), H / (H ∩ No) ≌ (H G.* No) / No, (H G.* No) / No ⊆ G / No■
),

[" 
 	Let G be a group, and N a normal subgroup of G. Then

	1.	If K is a subgroup of G such that  N ⊆ K ⊆ G, then K/N is a subgroup of G/N.
	2.	Every subgroup of G/N is of the form K/N, for some subgroup K of G such that  N ⊆ K ⊆ G.
	3.	If K is a normal subgroup of G such that  N ⊆ K ⊆ G, then K/N is a normal subgroup of G/N.
	4.	Every normal subgroup of G/N is of the form K/N, for some normal subgroup K of G such that  N ⊆ K ⊆ G.
	5.	If K is a normal subgroup of G such that  N ⊆ K ⊆ G, then the quotient group (G/N)/(K/N) is isomorphic to G/K.
 "]
s"Third Isomorphism Theorem":
(
	@giacomo.h.group_theory,
	∀No: ∈ normal(G), D: = {H|∀H: %subgroup% G, No ⊆ H}, C: = {∀J: %subgroup% G/No},
	∃φ: ∈ bijection, φ.D = D, φ.C = C,
	∀H: ∈ D, φ(H) = H / No■

	@giacomo.h.group_theory,
	∀G: ∈ group, ∀No: ∈ normal(G), ∀H: %subgroup% G,
	No ⊆ H ⇒
	(H / No ∈ normal(G/No) ⇔ H ∈ normal(G))■

	@giacomo.h.group_theory,
	∀G: ∈ group, ∀No: ∈ normal(G), ∀H: (%subgroup% G, No ⊆ H),
	H ∈ normal(G) ⇒ (G/N)/(H/N) ≌ G/H■
),

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀H: ∈ group, ∀f: ∈ homomorphism,
	∀(f.D = G, f.C = H) ⇒
	(
		ker(f) = {G.e} ⇒ f ∈ injection
	)■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G), *: = &G.*&, e: = G.e,
	∀(H ∩ K = {e}, H * K = G) ⇒ H × K ≌ G■
; Theorem: end
; Proof: start
	; Lemma: start
		@giacomo.h.group_theory,
		∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G), *: = &G.*&, e: = G.e,
		H ∩ K = {e} ⇒ (∀h: ∈ H, ∀k: ∈ K, h * k = k * h)■
	; Lemma: end
	; Proof: start
		@giacomo.h.group_theory,
		∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G), *: = &G.*&,
		H ∩ K = {e} ⇒
		(
			∀h: ∈ H, ∀k: ∈ K,
			(
				∃h': ∈ H, k * h * k^-1 * h^-1 = (k * h * k^-1) * h^-1 = h' * h^-1 ∈ H,
				∃k': ∈ K, k * h * k^-1 * h^-1 = k * (h * k^-1 * h^-1) = k * k' ∈ K
			)
			⇒ k * h * k^-1 * h^-1 ∈ H ∩ K = {e} ⇒ h * k = k * h
		)■
	; Proof: end
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G),
	*: = &G.*&, e: = G.e,
	∃φ: ∈ mapping, φ.D = H × K, φ.C = G, f(∀h: ∈ H, ∀k: ∈ K) = h * k,
	(
		φ ∈ homomorphism ⇐
		(
			∀(h1, h2): ∈ H^2, ∀(k1, k2): ∈ K^2,
			φ(h1 * h2, k1 * k2) = h1 * h2 * k1 * k2 = h1 * k1 * h2 * k2
		),
		φ ∈ injection ⇐
		(
			∀(h, k): ∈ H × K, φ(h, k) = e ⇒ h * k = e ⇒ h = k^-1 ⇒ ∀(h ∈ K, k ∈ H) ⇒ ∀(h ∈ {e}, k ∈ {e}) ⇒ (h, k) = (e, e) ⇒ ker(φ) = {(H × K).e}
		),
		φ ∈ surjection ⇐
		(
			φ(H × K) = {h * k|∀(h, k): ∈ H × K} = H * K = G
		)
	)
	⇒ φ ∈ isomorphism ⇒ H × K ≌ G■
; Proof: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G),
	*: = &G.*&, e: = G.e,
	H ∩ K = {e} ⇒
	(
		∃f: ∈ homomorphism, f.D = H × K, f.C = G, f ∈ injection
	)■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group,
	∀S: ∈ conjugate(G), {G.e} ∈ S■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group,
	∀H: ∈ normal(G),
	∃τ: ⊆ {∀S: ∈ conjugate(G)}, H = ∪(τ)■
; Theorem: end

; Theorem: start
	@giacomo.h.group_theory,
	∀G: ∈ group,
	∀H: ∈ normal(G),
	∃τ: ⊆ {∀S: ∈ conjugate(G)}, H = ∪(τ)■
; Theorem: end

p"Jordon-Holder Theorem":
(
	@giacomo.h.group_theory,
	∀G: ∈ finite_group,
	∀A: ∈ composition_series(G),
	∀B: ∈ composition_series(G),
	|A| = |B|
	∃f: ∈ bijection, f.D = A, f.C = B, ∀S: ∈ A, A ≌ f(A)■
)

; Theorem: start
	@giacomo.h.group_theory,
	A[5] ∈ solvable_group,
	S[∀n: ∈ {3, 4}] ∈ solvable_group,
	!(S[∀n: (∈ N, n ≥ 5)] ∈ solvable_group)
	∀G: ∈ group, |G| ∈ prime_number ⇒ G ∈ solvable_group■
; Theorem: end
