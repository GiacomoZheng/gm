: ; unique_factorization_ring
(
	"the integal ring in which every non-zero element not a unit can be written as the product of finite unique irreducible polynomial"
	⊆ integral_ring,
	...
	∀U: ∈ unique_factorization_ring,
	
)