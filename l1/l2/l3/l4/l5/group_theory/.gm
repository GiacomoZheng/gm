^:
(
	∈ 
)

≌:
(
	∈ relator,

	∀A: ∈ group, ∀B: ∈ group,
	(A ≌ B) ⇔ (∃f: ∈ isomorphism, ∀(f.D = A, f.C = B)) ; ? undetermined
),

%subgroup%:
(
	∈ relator,

	∀H: ∈ group, ∀G: ∈ group,
	(H %subgroup% G) ⇔ ∀(H ⊆ G, &H.*& = &G.*&, H.e = G.e)
),
[" 
	```
	similarly,
	(
		∈ relator,
		∀G: ∈ group, ∀H: ⊆ G,
		(H %subgroup% G) ⇔
		∀(
			%H.*% = %G.*%,
			∀(x, y): ∈ H^2, H.e = G.e, x H.* y ∈ H,
			∀z: ∈ H, z^-1 ∈ H
		)
	)
	```
 "]

/:
(
	∈ operator,

	∀G: ∈ group, ∀H: %subgroup% G,
	*: = &G.*&
	(G / H): = {{x * h|∀h: ∈ H}|∀x: ∈ G} ; the set of all the left cosets of H
),

order[∀G: ∈ group](∀x: ∈ G):
(
	D: = {m|∀m: ∈ N, G.e (G.* x)^m = G.e}
	!(D = ∅) ⇒ = inf(D)
),
; the domian of inf function do not contains ∅. If so, the error will occur

×:
(
	[" direct product "]
	∀A: ∈ group, ∀B: ∈ group,
	(A × B):
	(
		∈ group,
		D: = A.D × B.D,
		*:
		(
			∈ operator,
			∀(x1, y1): ∈ D, ∀(x2, y2): ∈ D, ((x1, y2) * (x2, y2)): = (x1 A.* x2, x2 B.* y2) 
		),
		e: = (A.e, B.e)
	)
),

gcd(∀m: ∈ N, ∀n: ∈ N): = sup({m / k, n / k|∀k: ∈ N} ∩ N),
lcm(∀m: ∈ N, ∀n: ∈ N): = inf({m × k, n × k|∀k: ∈ N})

prime_number: = {n|∀n: ∈ N, gcd(n, 1) = n}

gp[∀*: ∈ operator](∀S: ∈ set):
(
	[" if no error happens "]
	∈ group,
	D: = {x ^ m * y ^ n|∀x: ∈ S, ∀y: ∈ S, ∀m: ∈ N, ∀n: ∈ N}
	*: = &*&,
	e: ...
),


sym(∀S: ∈ set):
(
	∈ group,
	D: = {f|∀f: ∈ permutation, f.D = S}, ; N here don't contains 0
	*: = &∘&,
	e:
	(
		∈ permutation,
		D: = S,
		e(∀x: ∈ D): = x
	)
),

S[∀n: ∈ N]:
(
	D: = {m|∀m: ∈ N, m ≤ n},
	= sym(D)
),

c(∀a: ∈ sequence):
(
	∈ circle,
	Length: = a.cardinal,
	
),

orb[∀μ: ∈ action](∀s: ∈ μ.S): = {μ(g, s)|∀g: ∈ G},
stab[∀μ: ∈ action](∀s: ∈ μ.S): = {g|∀g: ∈ G, μ(g, s) = s},

centralizer[∀G: ∈ group](∀a: ∈ G): = {g|∀g: ∈ G, a G.* g = g G.* a},
center(∀G: ∈ group): = ∩[∀a: ∈ G]({centralizer[G](a)}),

A[∀n: ∈ N]: ; alternating group
(
	∈ group,
	D: = {f|∀f: ∈ S[n], f ∈ even_func},
	*: = &S[n].*&
	e: = S[n].e
),

D[∀n: ∈ N]: ; Dihedral group
(
	a[∀m: (∈ N, ≤ n)]: = m,
	σ: = c(a),
	b[∀m: (∈ N, ≤ n / 2)]: = c(m, 2 + n - m)
	B[0]: i,
	B[∀m: (∈ N, ≤ n / 2)]: = B[m - 1] ∘ b[m],
	τ: = B[sup({∀m: (∈ N, ≤ n / 2)})],
	= gp({σ, τ})
),

normal(∀G: ∈ group):
(
	⊆ ◉,

	*: = &G.*&

	∀Nor: ∈ normal(G),
	Nor %subgroup% G,
	∀h: ∈ Nor, ∀g: ∈ G, g*h*g^-1 ∈ Nor
),


ker(∀f: ∈ homomorphism): = {x|∀x: ∈ f.D, f(x) = f.C.e},

conjugate(∀G: ∈ group):
(
	⊆ ◉,

	*: = &G.*&

	∀S: ∈ conjugate(G),
	∀h: ∈ S, S = {g*h*g^-1|∀g: ∈ G}
),

composition_series(∀G: ∈ finite_group): ; !
(
	⊆ set,

	Length: ∈ N,

	∀C: ∈ composition_series(G),
	H[∀n: ∈ {m|∀m: ∈ N, m ≥ 0, m ≤ C.Length}]: ⊆ G,
	H[0] = {G.e}, H[C.Length] = G,
	∀n: ∈ {m|∀m: ∈ N, 0 ≤ m < C.Length},
	H[n] ∈ normal(H[n + 1]),
	H[n + 1] / H[n] ∈ simple_group,
	C = {H[∀n: ∈ {m|∀m: ∈ N, m ≥ 0, m ≤ C.Length}]}
),

