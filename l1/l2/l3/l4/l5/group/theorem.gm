; a weeker criterion of group
	s"the semigroup with same side identity and inverse is a group":
	(
		r'_group:
		(
			⊆ semigroup,
			∀G: ∈ r'_group, !(G = ∅),
			G.e: ∈ G,
			*: = &G.*&,
			∀g: ∈ G, g * G.e = g, ; right identity
				∃g': ∈ G, g * g' = G.e ; right inverse
		),
		l'_group:
		(
			⊆ semigroup,
			∀G: ∈ l'_group, !(G = ∅),
			G.e: ∈ G,
			*: = &G.*&,
			∀g: ∈ G, G.e * g = g, ; left identity
				∃g': ∈ G, g' * g = G.e ; left inverse
		),
		l'_group = group = r'_group
	),
	[ proof with the example of r'group```
		∀R: ∈ r'_group, *: = &R.*&, e: = G.e,
		∀g: ∈ G, ∃g': ∈ G, g * g' = e,
		∃g'': ∈ G, g' * g'' = e,
		g' * g = e ⇐
		(
			g' * g = g' * (g * e) = g' * (g * (g' * g'')) = (g' * (g * g')) * g'' = (g' * e) * g'' = g' * g'' = e
		),
		e * g = g ⇐
		(
			e * g = (g * g') * g = g * (g' * g) = g * e ; due to the proof above, the left inverse is also the right one
		)
	``` ]
; end

; about the examples
	s"direct product of two groups is also a group":
	(
		@gm.h.Group,
		∀A: ∈ group, ∀B: ∈ group, C: = A × B,
		C ∈ group, ∀(x1, y1): ∈ C, ∀(x2, y2): ∈ C,
		(x1, y1) C.* (x2, y2) = (x1 A.* x2, y1 B.* y2)■
	),

	s"symmetric group":
	(
		@gm.h.Group,
		∀S: ∈ set, i(∀x: ∈ S): = x,
		sym(S) ∈ group, &sym(S).*& = &∘&, sym(S).e = i■
	),
	[ proof```
		∀{α, β}: ∈ sym(S),
		&∘& ∈ associative_operator,
		α ∘ β ∈ sym(S),
		∀(
			∀x: ∈ S,
			(α ∘ i)(x) = α(i(x)) = α(x), 
			(i ∘ α)(x) = i(α(x)) = α(x)
		)
		⇒ α ∘ i = α = i ∘ α ⇒ i = sym(S).e,
		∃α^-1: ∈ sym(S), ; bijection
		α ∘ α^-1 = e = α^-1 ∘ α
	``` ]

	s"linear group":
	(
		@gm.h.Group,
		∀n: ∈ Np, ∀V: ∈ vector_space,
		ex.GL[n](V) ∈ group, &GL[n](V).*& = &∘&, ; here the matrices are regraded functions
		ex.SL[n](V) ∈ group, &SL[n](V).*& = &∘&■
	),
; end

s"uniqueness in group": ; +
(
	@gm.h.Group,
	∀G: ∈ group, *: = &G.*&, e: = G.e
	∀i: ∈ G, (∀g: ∈ G, i * g = g * i = g) ⇒ i = e, ; ideneity
	∀a: ∈ G, ∃a': ∈ G, a * a' = a' * a = e,
		∀g: ∈ G, a * g = g * a = e ⇒ g = a' ; inverse
	■
),
[ proof```
	"indentity:", ∀i: ∈ G, (∀g: ∈ G, i * g = g * i = g) ⇒ (i = i * e = e * i = e) ⇒ i = e,
	"inverse:", ∀a: ∈ G, ∃a': ∈ G, ∀g: ∈ G, a * g = g * a = e
	⇒ a' = a' * (a * g) = a' * a * g = (a' * a) * g = g ⇒ g = a'
``` ]

s"inverse":
(
	@gm.h.Group,
	∀G: ∈ group, ∀a: ∈ G,
	inv[G](inv[G](a)) = a ; the inverse of inverse is itself
	∀b: ∈ G, *: = &G.*&,
	inv[G](a * b) = inv[G](b) * inv[G](a) ; be careful about the order
	■
),
[ proof```
	∀G: ∈ group, ∀a: ∈ G, ∀b: ∈ G, *: = &G.*&,
	"inverse of inverse:",
	a = a * e = (a * inv[G](a)) * inv[G](inv[G](a)) = e * inv[G](inv[G](a)) = inv[G](inv[G](a)), ; because of two-side inverse and two-side identity
``` ]

s"cancellation in a group":
(
	@gm.h.Group,
	∀G: ∈ group, ∀a: ∈ G, *: = &G.*&,
	∀{u, v}: ⊆ G,
	a * u = a * v ⇔ u = v ⇔ u * a = v * a■ ; because the inverse of every element exist
),

s"the numbers leading 0 divides the order":
(
	@gm.h.Group,
	∀G: ∈ group, *: = &G.*&, e: = G.e,
	∀x: ∈ G, ∀n: ∈ s.Z, e (* x)^n = e ⇒ n / order[G](x) ∈ s.Z■
),
[ proof```
	m: = order[T](x),
	∃{q, r}: ⊆ s.Z, n = m × q + r, 0 ≤ r < m, ; the division theorem on Z ; assume it here
	e = e (* x)^n = e (* x)^(m × q + r) = e (* x)^(m × q) (* x)^r = e ((* x)^m)^q (* x)^r = e (* x)^r ⇒ r = 0
``` ]

; about the subgroup
	s"criterions of subgroup":
	(
		@gm.h.Group,
		∀G: ∈ group, ∀H: ⊆ G,
		H ∈ subgroup(G) ⇔
		∀(
			!(H = ∅),
			(∀{x, y}: ⊆ H, x G.* inv[G](y) ∈ H)
		)■
	),

	s"relation of subgroup (of) is transitive":
	(
		@gm.h.Group,
		∀G: ∈ group,
		∀H: ∈ subgroup(G),
		∀I: ∈ subgroup(H),
		I ∈ subgroup(G)■
	),

	s"any intersection of subgroups are still subgroup"

	s"centralizers, centers and normalizers are all subgroup":
	(
		@gm.h.Group,
		∀G: ∈ group, *: = &G.*&, e: = G.e,
		∀A: ⊆ G,
		C[G](A) ∈ subgroup(G),
		Z(G) ∈ subgroup(G),
		N[G](A) ∈ subgroup(G)
		C[G](A) ∈ subgroup(N[G](A))■
	),
	[ prove ```
		C[G](A) ∈ subgroup(G) ⇐
		(
			∀a: ∈ A,
			e * a = a * e ⇒ e ∈ C[G](A) ⇒ !(C[G](A) = ∅),
			∀{h, k}: ⊆ C[G](A),
			∀(inv[G](h) * a * h = a, inv[G](k) *  a * k = a)
			⇒ inv[G](h) * a * h = inv[G](k) *  a * k
			⇒ k * inv[G](h) * a = a * (k * inv[G](h))
			⇒ k * inv[G](h) ∈ C[G](A)
		),
		Z(G) = C[G](G),
		N[G](A) ∈ subgroup(G) ⇐
		(
			∀a: ∈ A,
			e * a = a * e ⇒ e ∈ N[G](A) ⇒ !(N[G](A) = ∅),
			∀{h, k}: ⊆ N[G](A),
			inv[G](h) * a * h ∈ A
			⇒ k * inv[G](h) * a * h * inv[G](k) ∈ A
			⇒ (k * inv[G](h)) * a * inv[G](k * inv[G](h)) ∈ A
			⇒ k * inv[G](h) ∈ N[G](A)
		),
		C[G](A) ∈ subgroup(N[G](A)) ⇐
		(
			C[G](A) ∈ group,
			C[G](A) ⊆ N[G](A),
			C[G](A).e = e = N[G](A).e,
			&C[G](A).*& = &N[G](A).*&
		)
	``` ]
; end

s"the orders in a group":
(
	@gm.h.Group,
	∀G: ∈ group, *: = &G.*&, e: = G.e,
	∀x: ∈ G, ∀m: (∈ s.Z, !(m = 0)),
	n: = order[G](x),
	!(n ∈ s.N) ⇒ !(order[G](e (* x)^m) ∈ s.N),
	n ∈ s.N ⇒ order[G](e (* x)^m) = n / gcd(n, m)■
),

s"relation between isomorphic groups":
(
	@gm.h.Group,
	∀φ: ∈ isomorphism, G: = φ.D, H: = φ.C,
	|G| = |H|,
	G ∈ abelian_group ⇔ H ∈ abelian_group,
	∀x: ∈ G, order[G](x) = order[H](φ(x))■
),

s"G is homomorphic to the set of permutations under its action"
(
	@gm.h.Group,
	∀σ: ∈ action,
	∀G: = σ.D,
	H: = {σ$x}[∀x: ∈ G],
	H ∈ group, &H.*& = &∘&,
	f(∀x: ∈ G): = σ$x,
	f ∈ homomorphism, f.D = G, f.C = H■ 
),
[ ```
	; first, prove the H is a group
	&∘& ∈ associative_operator,
	∀{x, y}: ⊆ G, {σ$x, σ$y} ⊆ H, *: = &G.*&,
	∀a: ∈ σ.S,
	(σ$x ∘ σ$y)(a) = σ(x, σ(y, a)) = σ(x * y, a) = σ$(x * y)(a), σ$(x * y) ∈ H,
	i = H.e, i ∈ H,
	(σ$x ∘ σ$inv(x))(a) = σ(x * inv(x), a) = a, inv(σ$x) = σ$inv(x)
	; Then the homomorphism
	f(x * y) = σ$(x * y) = σ$x ∘ σ$y = f(x) ∘ f(y)
``` ]

; about cyclic_group
	s"every cyclic group is an abelian group":
	(
		cyclic_group ⊆ abelian_group
	),
	[ proof```
		∀G: ∈ cyclic_group, ∃g: ∈ G, {G.e (G.*g)^z}[∀z: ∈ s.Z] = G,
		∀{a, b}: ⊆ G, ∃{m, n}: ∈ Z,
		a = G.e (G.*g)^m, b = G.e (G.*g)^n,
		(
			a G.* b = G.e (*g)^m G.* G.e (G.*g)^n = G.e (*g)^(m + n),
			b G.* a = G.e (*g)^n G.* G.e (G.*g)^m = G.e (*g)^(n + m)
		)
		⇒ a G.* b = b G.* a ⇒ G ∈ abelian_group
	``` ]
	s"the cardinal of the cyclic (sub)group is the order of generator":
	(
		@gm.h.Group,
		∀G: ∈ cyclic_group, *: = &G.*&, e: = G.e,
		∃g: ∈ G, {e (* g)^n}[∀n: ∈ s.Z] = G,
		|G| = order[G](g)■
	),
	[ proof```
		!(|G| ∈ s.N) ⇒ |G| = order[G](g) ; the cardinal of Z
		|G| ∈ s.N ⇒
		(
			S: = {n|0 ≤ n < order[G](g)},
			|G| = |{e (* g)^n}[∀n: ∈ s.Z]| = |{e (* g)^n}[∀n: ∈ S]| = |S| = order[G](g)
		),
	``` ]

	s"about the orders in a cyclic group":
	(
		∀G: ∈ cyclic_group, e: = G.e, *: = &G.*&,
		∃g: ∈ G, H = gp[&*&](g),
		order[G](g) = |H|,
		|H| ∈ s.N ⇒ ( ; finite
			n: = |H|,
			S: = {m|0 ≤ m < n}[∀m: ∈ s.Z], ; |S| = n
			∀(
				e (* g)^n = e,
				|{e (* g)^n}[∀m: ∈ S]| = n, ; 1, g, g * g, … e (* g)^(n-1), distinct
			)
		),
		|H| = |s.N| ⇒ ∀( ; infinite
			(∀n: ∈ s.Z, e (* g)^n = e ⇒ n = 0),
			; equivalently
			(∀n: ∈ s.Z, ∀m: ∈ s.Z, e (* g)^n = e (* g)^m ⇒ n = m)
		)
	),

	s"two cyclic group with the same cardinal is isomorphic":
	(
		∀G: ∈ cyclic_group, ∀H: ∈ cyclic_group,
		|G| = |H| ⇒ (∃f: ∈ isomorphism, ∀(f.D = G, f.C = H))
	),

	s""
; end

s"the parity of any element of S[n] is an invariant": ; + define the permutaion matrix before
(
	s"any element of S[n] can be written as a permutation matrix, whose determinant is +1 or -1":,
),

; Theorem: start
	@gm.h.Group,
	∀C: ∈ coset,
	∃~: ∈ equivalence_relator, &~&.D = C.G,
	∀a: ∈ C.G, a ~ b ⇔ C.G.e (C.G.* a)^-1 C.G.* b ∈ C.G ■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀m: ∈ s.N \ {0}, Z[m] = {{x + m × z}[∀z: ∈ s.Z]}[∀x: ∈ s.Z]
	∃𝒴: = {y| ∀y: ∈ C, y ^ m = 1},
	𝒴 ∈ group, &𝒴.*& = &+&, 𝒴.e = 1
	Z[m] ≌ 𝒴■
; Theorem: end

s"Lagrange Theorem":
(
	@gm.h.Group,
	G: ∈ group, H: ∈ subgroup(G),
	|G| ∈ s.N ⇒ |G|/|H| ∈ s.N ■
),
[ proof, ```
	; Lemma: start
		@gm.h.Group,
		G: ∈ group, H: ∈ subgroup(G), *: = &G.*&
		∀x: ∈ G, |H| = |x * H| ■
	; Lemma: end
	[ proof, ```
		@gm.h.Group,
		G: ∈ group, H: ∈ subgroup(G), *: = &G.*&
		∀x: ∈ G,
		∃φ[x]:
		(
			∈ mapping,
			D: = H, C: = x × H,
			φ[x](∀h: ∈ H) = x * h
		),
		(
			(
				∀h: ∈ H, ∀h': ∈ H,
				φ[x](h) = φ[x](h') ⇒ x * h = x * h' ⇒ h = h'
			)
			⇒ φ[x] ∈ injection,
			(
				∀y: ∈ φ[x].C,
				∃h: ∈ H, y = x * h, φ[x](h) = y
			)
			⇒ φ[x] ∈ surjection
		)
		⇒ φ[x] ∈ bijection
		⇒ |φ[x].C| = |φ[x]|
		⇒ |H| = |x * H| ■
	``` ]
	@gm.h.Group,
	G: ∈ group, H: ∈ subgroup(G), *: = &G.*&
	(∀x: ∈ G, G.e ∈ H ⇒ x ∈ x * H) ⇒ !(x * H = ∅),
	|G| ∈ s.N ⇒
	(
		∃m: (∈ s.N, ≤ |G|),
		|G| = |∪[]({x * H|∀x: ∈ G})|
		= |∪[]({x[n] * H|∀x[∀n: (∈ s.N, ≤ m)]: ∈ G})|
		= +[∀n: (∈ s.N, ≤ m)](|x[n] * H|)
		= |H| × m
	)■
``` ]

; Theorem: start
	@gm.h.Group,
	∀G: ∈ cyclic_group,
	∀H: ∈ subgroup(G),
	H ∈ cyclic_group ■
; Theorem: end

s"The converse of \"Lagrange\" when G is cyclic":
(
	@gm.h.Group,
	G: ∈ group, 
	|G| ∈ s.N ⇒ (∀d: ∈ s.N, m/d ∈ s.Z ⇒ (∃H: ∈ subgroup(G), |H| = d))■
),

; Theorem: start
	@gm.h.Group,
	∀m: (∈ s.N, > 1), ∀n: (∈ s.N, > 1),
	Z[m] × Z[n] ≌ Z[m × n] ⇔ gcd(m, n) = 1■
; Theorem: end

s"Fundamental Theorem of Finite generated abelian group":
(
	@gm.h.Group,
	∀G: ∈ finitely_generated_group,
	G ∈ abelian_group ⇒ (∃m: ∈ s.N, ∃T: ∈ abelian_group, |T| ∈ s.N, G ≌ Z^m × T)■
),
; Proof

s"Cayley's Theorem":
(
	@gm.h.Group,
	∀G: ∈ group, ∃T: ∈ subgroup(sym(G)), G ≌ T■
),
; similarly
; Theorem: strat
	@gm.h.Group,
	∀G: ∈ group, ∃T: ∈ subgroup(S[|G|]), G ≌ T■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀μ: ∈ action, G: = μ.G, stab[μ](∀s: ∈ μ.S) ∈ subgroup(G)■
; Theorem: end

s"Orbit-Stabilizer Theorem":
(
	@gm.h.Group,
	∀μ: ∈ action, ∀s: ∈ μ.S,
	|orbit[μ](s)| ∈ s.N ⇒ |G / stab[μ](s)| = |orbit[μ](x)|
	; with the help of "Lagrange Theorem", we get
	∀(|orbit[μ](s)| ∈ s.N, |μ.G| ∈ s.N) ⇒ |G| = |stab[μ](s)| × |orbit[μ](x)|
	■
),

s"Sylow's First Theorem":
(
	@gm.h.Group,
	∀G: ∈ group,
	|G| ∈ s.N ⇒
	(
		∀p: ∈ prime_number, ∀n: ∈ s.N, m: = |G|/(p^n),
		∀(m ∈ s.N, gcd(m, p) = 1) ⇒
		(
			∃H: ∈ subgroup(G), |H| = p^n
		)
	)■
),

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group, center(G) ∈ subgroup(G)■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group, ∀p: ∈ prime_number, ∀n: ∈ s.N,
	|G| = p^n ⇒ !(center(G) = {G.e})■
; Theorem: end

; * About S[n], A[n]
; Theorem: start
	@gm.h.Group,
	∀n: ∈ s.N,
	|S[n]| = fac(n), ; n * (n - 1) * … * 1
	S[n] = gp({c(a, b)|∀(a, b): ∈ s.N^2, a ≤ n, b ≤ n}),
	A[n] = gp({c(x, y, z)|∀(x, y, z): ∈ s.N^3, x ≤ n, y ≤ n, z ≤ n})■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group,
	G ∈ abelian_group ⇔ (∀H: ∈ subgroup(G), H ∈ normal(G))■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group, ∀No: ∈ normal(G),
	∃H: ∈ group, H = G / No, 
	H.*
	(
		∈ symbol, ∀(x, y): ∈ (G / No)^2,
		(x G.* No) * (y G.* No) = (x * y) G.* No
	)■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group, ∀No: ∈ normal(G),
	∀H: ∈ subgroup(G), No ⊆ H ⇒ No ∈ normal(H)■
; Theorem: end

s"First Isomorphism Theorem": ; !
(
	∀f: ∈ homomorphism, ∃F: ∈ isomorphism,
	F.D = f.D/ker(f), F.C = Range(f),
	∀x: ∈ f.D, F(x G.* ker(f)) = f(x),
	; what's more
	|G| = |ker(f)| × |Range(f)|
),

s"Second Isomorphism Theorem":
(
	@gm.h.Group,
	∀G: ∈ group, ∀H: ∈ subgroup(G), ∀No: ∈ normal(G),
	(H ∩ No) ∈ normal(G), H / (H ∩ No) ≌ (H G.* No) / No, (H G.* No) / No ⊆ G / No■
),

[ 
 	Let G be a group, and s.N a normal subgroup of G. Then

	1.	If K is a subgroup of G such that  s.N ⊆ K ⊆ G, then K/s.N is a subgroup of G/s.N.
	2.	Every subgroup of G/s.N is of the form K/s.N, for some subgroup K of G such that  s.N ⊆ K ⊆ G.
	3.	If K is a normal subgroup of G such that  s.N ⊆ K ⊆ G, then K/s.N is a normal subgroup of G/s.N.
	4.	Every normal subgroup of G/s.N is of the form K/s.N, for some normal subgroup K of G such that  s.N ⊆ K ⊆ G.
	5.	If K is a normal subgroup of G such that  s.N ⊆ K ⊆ G, then the quotient group (G/s.N)/(K/s.N) is isomorphic to G/K.
 ]
s"Third Isomorphism Theorem":
(
	@gm.h.Group,
	∀No: ∈ normal(G), D: = {H|∀H: ∈ subgroup(G), No ⊆ H}, C: = {∀J: ∈ subgroup(G)/No},
	∃φ: ∈ bijection, φ.D = D, φ.C = C,
	∀H: ∈ D, φ(H) = H / No■

	@gm.h.Group,
	∀G: ∈ group, ∀No: ∈ normal(G), ∀H: ∈ subgroup(G),
	No ⊆ H ⇒
	(H / No ∈ normal(G/No) ⇔ H ∈ normal(G))■

	@gm.h.Group,
	∀G: ∈ group, ∀No: ∈ normal(G), ∀H: (∈ subgroup(G), No ⊆ H),
	H ∈ normal(G) ⇒ (G/s.N)/(H/s.N) ≌ G/H■
),

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group, ∀H: ∈ group, ∀f: ∈ homomorphism,
	∀(f.D = G, f.C = H) ⇒
	(
		ker(f) = {G.e} ⇒ f ∈ injection
	)■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G), *: = &G.*&, e: = G.e,
	∀(H ∩ K = {e}, H * K = G) ⇒ H × K ≌ G■
; Theorem: end
[ proof, ```
	; Lemma: start
		@gm.h.Group,
		∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G), *: = &G.*&, e: = G.e,
		H ∩ K = {e} ⇒ (∀h: ∈ H, ∀k: ∈ K, h * k = k * h)■
	; Lemma: end
	[ proof, ```
		@gm.h.Group,
		∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G), *: = &G.*&,
		H ∩ K = {e} ⇒
		(
			∀h: ∈ H, ∀k: ∈ K,
			(
				∃h': ∈ H, k * h * k^-1 * h^-1 = (k * h * k^-1) * h^-1 = h' * h^-1 ∈ H,
				∃k': ∈ K, k * h * k^-1 * h^-1 = k * (h * k^-1 * h^-1) = k * k' ∈ K
			)
			⇒ k * h * k^-1 * h^-1 ∈ H ∩ K = {e} ⇒ h * k = k * h
		)■
	``` ]
	@gm.h.Group,
	∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G),
	*: = &G.*&, e: = G.e,
	∃φ: ∈ mapping, φ.D = H × K, φ.C = G, f(∀h: ∈ H, ∀k: ∈ K) = h * k,
	(
		φ ∈ homomorphism ⇐
		(
			∀(h1, h2): ∈ H^2, ∀(k1, k2): ∈ K^2,
			φ(h1 * h2, k1 * k2) = h1 * h2 * k1 * k2 = h1 * k1 * h2 * k2
		),
		φ ∈ injection ⇐
		(
			∀(h, k): ∈ H × K, φ(h, k) = e ⇒ h * k = e ⇒ h = k^-1 ⇒ ∀(h ∈ K, k ∈ H) ⇒ ∀(h ∈ {e}, k ∈ {e}) ⇒ (h, k) = (e, e) ⇒ ker(φ) = {(H × K).e}
		),
		φ ∈ surjection ⇐
		(
			φ(H × K) = {h * k|∀(h, k): ∈ H × K} = H * K = G
		)
	)
	⇒ φ ∈ isomorphism ⇒ H × K ≌ G■
``` ]

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group, ∀H: ∈ normal(G), ∀K: ∈ normal(G),
	*: = &G.*&, e: = G.e,
	H ∩ K = {e} ⇒
	(
		∃f: ∈ homomorphism, f.D = H × K, f.C = G, f ∈ injection
	)■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group,
	∀S: ∈ conjugate(G), {G.e} ∈ S■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group,
	∀H: ∈ normal(G),
	∃τ: ⊆ {∀S: ∈ conjugate(G)}, H = ∪[](τ)■
; Theorem: end

; Theorem: start
	@gm.h.Group,
	∀G: ∈ group,
	∀H: ∈ normal(G),
	∃τ: ⊆ {∀S: ∈ conjugate(G)}, H = ∪[](τ)■
; Theorem: end

p"Jordon-Holder Theorem":
(
	@gm.h.Group,
	∀G: ∈ finite_group,
	∀A: ∈ composition_series(G),
	∀B: ∈ composition_series(G),
	|A| = |B|
	∃f: ∈ bijection, f.D = A, f.C = B, ∀S: ∈ A, A ≌ f(A)■
)

; Theorem: start
	@gm.h.Group,
	A[5] ∈ solvable_group,
	S[∀n: ∈ {3, 4}] ∈ solvable_group,
	∀n: (∈ s.N, n ≥ 5), !(S[n] ∈ solvable_group),
	∀G: ∈ group, |G| ∈ prime_number ⇒ G ∈ solvable_group■
; Theorem: end
