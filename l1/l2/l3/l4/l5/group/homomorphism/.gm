: ; homomorphism
(
	⊆ mapping,

	∀f: ∈ homomorphism,
	f.D ∈ group, f.C ∈ group,
	∀x: ∈ f.D, ∀y: ∈ f.D,
	f(x f.D.* y) = f(x) f.C.* f(y)
),
[ ```
	∀a: ∈ G,
	e * f(a) = f(a) = f(e * a) = f(e) * f(a) ⇒ f(e) = e
	e = f(e) = f(a * a') = f(a) * f(inv(a)) ⇒ f(inv(a)) = inv(f(a))
``` ]