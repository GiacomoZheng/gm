: ; circle
(
	⊆ function,

	∀f: ∈ circle,
	f.Length: ∈ s.N,
	S: = {n|0 ≤ n < f.Length},

	∃a[∀n: ∈ S]: ∈ ◉, 
	∀n: ∈ S\{0}, f(a[n - 1]) = a[n],
	f(a[f.Length - 1]) = a[0],

	∀x: ∈ ◉, 
	!(
		∃n: ∈ S, x = a[n]
	)
	⇒ f(x) = x ; anything else
),