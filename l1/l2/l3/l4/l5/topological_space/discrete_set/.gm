: ; discrete_set
(
	⊆ topological_space,
	∀D: ∈ discrete_set,
	∀{x, y}: ⊆ D, !(x = y) ⇒ ∀(!(x D.→ y), !(y D.→ x))
	[ equivalently
		```∃(x D.→ y, y D.→ x) ⇒ x = y```
		or
		```D.τ = Ps(D)```
		or
		```∀x: ∈ D, {x} ∈ D.τ```
	]
),