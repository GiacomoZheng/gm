: ; field_extension ; !
(
	⊆ unity_ring.homomorphism,

	∀f: ∈ field_extension,
	f.D ∈ field, f.C ∈ field,

	f.degree: = |f.C / f.D| ; in fact, this is just a group on "+", as normal
),
[ 
	it is true that a field_extension is always an injection,
	so that the D is isomorphic to a subfield of C
 ]