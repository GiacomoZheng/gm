: ; polynomial_ring, in fact, this class won't be used frequently. we will use the P[]() instead
(
	⊆ unity_ring,
	x: ∈ ◉,
	R: ∈ unity_ring,
	degree: ∈ s.Z,

	∀Poly: ∈ polynomial_ring,
	Poly.degree ≥ 0, ; ! 0 has no degree

	∃SupR: ∈ unity_ring, Poly.R ∈ subring(SupR), Poly.x ∈ SupR,

	∀f(Poly.x): ∈ Poly,
	S: = {m}[∀m: ∈ s.Z, m ≥ 0, m ≤ Poly.degree]
	a[∀n: ∈ S]: ∈ Poly.R, ; ! should be method
	!(a[Poly.degree] = R.0),
	f(Poly.x) = +[∀n: ∈ S](a[n] (Poly.R.× Poly.x) ^ n),

	a[∀n: !(n ∈ S)]: = R.0
)