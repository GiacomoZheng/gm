: ; unique_factorization_ring, or unique factorization domain
; !
(
	"the integal unity_ring in which every non-zero element not a unit can be written as the product of finite unique irreducible polynomial"
	⊆ integral_ring,
	...
	∀U: ∈ unique_factorization_ring,
	
)