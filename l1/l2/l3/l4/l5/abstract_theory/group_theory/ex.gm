Np: = {n}[∀n: ∈ s.Z, n > 0]

; group of roots of 1
U[∀n: ∈ Np]: = {ξ^m}[∀m: (∈ s.Z, 0 ≤ m < n), ζ: (∈ s.C, ζ^n = 1)],

; Dihedral group ; !
D[∀n: ∈ Np]: = gp[*: ∈ operator]({σ, τ}[∃σ: ∈ ◉, ∃τ: ∈ ◉, order[&*&](σ) = n, order[&*&](τ) = 2, σ * τ = τ (* σ)^2]),
[```
	σ: = D[n].σ, τ: = D[n].τ,
	D[n] = gp({σ∘τ, τ}) ⇐ σ∘τ ∘ τ = σ,
	order[D[n]](σ) = n,
	order[D[n]](τ) = 2,
	τ∘σ = σ^-1 ∘ τ
``` ]
Qu: ; Quaternion group ; !
= {1, -1, i, -i, j, -j, k, -k}[{1, i, j, k}:] ; +