^:
(
	∈ symbol,
	∀n: ∈ integer, ; undefined in july 4

	∀f: ∈ permutation,
	∃i:
	(
		∈ permutation,
		i(∀x: ∈ f.D)= x,
	),
	(f^0) = i,
	n > 0 ⇒ (f^n) = f ^ (n - 1) ∘ f,
	n < 0 ⇒ (f^n) = f ^ (n + 1) ∘ f ^ -1 ; inverse, undefined on July 4
),

conj(∀x: ∈ s.C): = ...