: ; coset, left coset in fact
(
	⊆ set,

	G: ∈ group, ; original group
	H: %subgroup% G,
	g: ∈ G, ; generator

	∀C: ∈ coset,
	*: = &C.G.*&, ; as C is a varibale, the "*" here is a variable too
	C = {g * h| ∀h: ∈ H} ; g in the left part
)