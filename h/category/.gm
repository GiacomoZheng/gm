: ; category
(
	⊆ class.sub(set), ; a category is a subclass of set
	(
		∀c: ∈ category,
		c.morphism: ⊆ set.morphism, ; + now I think it should be like a
		∀f: ∈ c.morphism, f.D ∈ c, f.C ∈ c,
		; I'm considering about something called natrural subsets (like topology)

		; attributes
			; +
	),
	; location
		; +
),