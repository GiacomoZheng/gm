; + 
"power set", "poset", "maximal",
; Theorem: start
	∀X: ∈ set, Ps(X) ∈ poset, &PS(X).≤& = &⊆&,
	X ∈ maximal(G) ; in fact, it is the only one
; Theorem: end

"finite set", "posoet", "maximal",
; Theorem: start
	∀P: ∈ poset,
	|P| ∈ h.N ⇒ (∃x: ∈ P, x ∈ maximal(G)),
; Theorem: end

