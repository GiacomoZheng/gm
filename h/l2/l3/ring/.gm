: ; ring, with multiplication identity
(
	⊆ rng,

	∀R: ∈ ring,
	R.1: ∈ R, ; ? I may request it as nonzero later
	∀a: ∈ R, R.1 R.× a = a = a R.× R.1, ; multiply identity
	
	; location
		; about alias
			commutative_domain: = integral_domain,
			integral_ring: = integral_domain, ; ? I may delete it later
		; end

		; + generalize
			sub(∀R: ∈ ring):
			(
				⊆ ring,
				⊆ rng.sub(R), ; the same "×"

				∀H: ∈ sub(R),
				H.1 = G.1
			),
		; end

		neg[∀R: ∈ ring]: = rng.neg[R],
		
		unit(∀R: ∈ ring):
		(
			⊆ ◉,
			∀x: ∈ unit(R),
			x ∈ R,
			∃x': ∈ R, x' R.× x = R.1 = x R.× x'
		),

		inv[∀R: ∈ ring](∀x: ∈ unit(R)):
		(
			"valid because of the inverse is unique",
			∃x': ∈ R, x' R.× x = R.1 = x R.× x',
			= x'
		),
),