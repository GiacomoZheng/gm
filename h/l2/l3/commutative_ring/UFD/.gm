: ; UFD, unique_factorization_domain
; !
(
	"the integal domain in which every non-zero element not a unit can be written as the product of finite unique irreducible factorization",
	⊆ integral_domain,
	... ; +
	∀U: ∈ unique_factorization_ring,
	[ chaim: integral_domain ⊇ GCD_domain ⊇ UFD ]
	; location
		; about inheritance
			divisor[∀R: ∈ UFD]: = commutative_rng.divisor[R],

			multiple[∀R: ∈ UFD]: = commutative_rng.multiple[R],

			associated[∀R: ∈ UFD]: = integral_domain.associated[R],

			common_divisor[∀R: ∈ UFD]: = commutative_rng.common_divisor[R],

			gcd[∀R: ∈ UFD]: = GCD_domain.gcd[R], ; here it is unique under associated

			irreducible(∀R: ∈ UFD): = integral_domain.irreducible(R), ; element

		; end
),
[ chaim: UFD ⊆ integral_domain (ring) ⊆ commutative_ring ]