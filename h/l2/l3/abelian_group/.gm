: ; abelian_group
(
	⊆ group,
	(
		∀G: ∈ abelian_group,
		&G.*& ∈ commutative_operator, G ⊆ &G.*&.D,

		G.+: = &G.*&, ; for convince
		G.0: = G.e, ; for convince

		G.-: ; ?
		(
			∈ operator,
			∀{a, b}: ⊆ G,
			a G.- b = a G.+ group.inv[G](b)
		)
	),

	; abut generalizing
		sub(∀G: ∈ abelian_group): = group.sub(G),
	; end

	norm: ; +
	(
		⊆ function,
		∀p: ∈ norm,
		p.D: ∈ abelian_group,
		; @h.R, ; the R, ≥ are all in the real number
		@p.D,
			∀x: ∈ D,
			p(x) ∈ h.R,
			!(x = 0) ⇒ p(x) h.R.> h.R.0, ; !
			p(x + x) = p(x) + p(x),
			p(neg[D](x)) = neg[h.R](p(x))
			∀{u, v}: ⊆ D, p(u + v) ≥ p(u) + p(v),

		■
		; ■
	)
),
[ chaim: abelian group ⊆ group ]