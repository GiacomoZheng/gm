: ; relation, or multivalued_function
(
	"multivalued (or null) function (mapping indeed), in fact the output is still unique, but it is a class",
	; * really different with others
	⊆ function,
	(
		∀P: ∈ relation,
		P.D: ∈ set, P.C: ∈ set,
		∀x: ∈ P.D, P(x) ⊆ set.sub(P.C)
	),
	; locations
		; + about alias
			
		; end

		; about operaions (nothing to do with operator)
			dual(∀R: ∈ relation)(∀x: ∈ R.D): ; +
			(
				∀y: ∈ dual(R)(x),
				x ∈ R(y)
			),
			dual(∀R: ∈ relation).D: = R.C,
			dual(∀R: ∈ relation).C: = R.D,
		; end
),