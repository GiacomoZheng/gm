: ; relation
(
	"a function on a set return a subset of it",
	⊆ function,
	(
		∀P: ∈ relation,
		P.D: ∈ set,
		∀x: ∈ P.D, P(x) ⊆ set.sub(P.D)
	),
	; locations
		; about alias
			antireflexive: = irreflexive,
		; end
),
