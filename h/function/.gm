: ; + function
(
	⊆ ◉,
	(
		"for every input, a function would give an unique output",
		; maybe all the inputs undefined can output empty class
	),

	; locations
		; about alias
		; end

		i(∀x: ∈ ◉): = x,
		id: = i,

		∘: , ; +
),
