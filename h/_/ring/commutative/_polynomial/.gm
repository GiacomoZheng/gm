: ; ! polynomial
(
	⊆ function,
	(
		∀f: ∈ polynomial,
		f.D: ; !
		(
			∈ commutative_ring.module,
			∈ commutative_ring
		),
		f.R: = f.D.R,
		@ℕ,
			∃n: ∈ ℕ, S: = {m|0 ≤ m < n}[∀m: ∈ ℕ],
			f.c[∀m: ∈ S]: ∈ f.R
		■
		@f.D,
			∀x: ∈ f.D,
			f(x) = +[∀m: ∈ S](π[f.c[m]](x ^ m))
		■
		; attributes
			f.trivial(∀x: ∈ ◉): = f.R.0, ; ?
			f.zero: = f.trivial
	),
	; location
		deg(∀f: ∈ polynomial): ; +
		(
			"the power of maximal nonzero item",

		),

		root(∀f: ∈ polynomial):
		(
			⊆ set.element(f.R),
			∀α: ∈ root(f),
			f(α) = R.0
		),
),
