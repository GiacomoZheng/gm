: ; module, regular module here, or named as left module
(
	"ring module",
	∈ structure.full_sub(rng.module), ; ? full
	; ⊆ rng.module,
	(
		∀M: ∈ module,
		M.π ∈ action ; here it is the ring (left) action
	)
),
