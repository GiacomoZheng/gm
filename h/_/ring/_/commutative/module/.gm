: ; ring.module
(
	; + I would define a "module-like" struture to omit these repeated parts
	∈ structure.full_sub(rng.module),
	(
		∀M: ∈ module,
		M.π ∈ action ; here it is the commutative_ring (left) action
	)
),
