: ; poset, or partial ordered set
(
	⊆ preordered_set,
	(
		∀S: ∈ poset,
		∀a: ∈ S, ∀b: ∈ S,
		[ not all the elements are comparable ]
		∀(a S.≤ b, b S.≤ a) ⇒ a = b ; antisymmetric
	),

	; location
		maximal_ideal(∀P: ∈ poset): ; diiferent from the biggest one
		(
			⊆ set.element(P),

			∀x: ∈ maximal_ideal(G),
			!y: ∈ G, !(x = y) ⇒ x G.≤ y
			[ equivalently```
				∀y: ∈ G, !(x = y) ⇒ !(x G.≤ y)
			``` ]
		),

		chain(∀P: ∈ poset):
		(
			⊆ set.sub(P),
			⊆ ordered_set ; ? total_ordered set
		),
),