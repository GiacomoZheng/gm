: ; weakly_increasing_function
(
	⊆ set.homomorphism, ; ? may change to preorder later
	(
		∀f: ∈ weakly_increasing_function,
		@f,
			D ∈ poset, C ∈ poset,
			∀{a, b}: ⊆ D,
			a D.≤ b ⇒ f(a) C.≤ f(b)
		■
	)
),