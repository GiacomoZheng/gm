: ; magma
(
	⊆ set,
	(
		∀S: ∈ magma,
		S.operation: ∈ operation,

		S ∈ S.operation.closure,

		; attributes
			S.*: (∈ operator, &S.*&.operation = S.operation)
	),
	; location
		; about inheritance
			idempotence(∀S: ∈ magma):
			(
				⊆ set.element(S),
				∀x: ∈ S.idempotence,
				x S.* x = x
			),

			; ! variable(∀S: ∈ magma):
			(
				⊆ set.sub(S), ; Intuitively, the domain for it to pick some variable
				; +
			)
		; end
),