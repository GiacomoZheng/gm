: ; open_map
(
	"the mappings maps open sets to opens sets",
	⊆ mapping,
	∀f: ∈ open_map,
	@f,
		D ∈ topological_space, C ∈ topological_space, ; ? I may create a new name for it
		∀O: ∈ D.τ, image(O) ∈ C.τ
	■
),
[ properties: continuous open map is homeomorphism ] ; +
