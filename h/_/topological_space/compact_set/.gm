: ; + compact_set
(
	⊆ topological_space,
	∀𝒞: ∈ open_cover(X),
	∃𝒞': (⊆ 𝒞, |𝒞'| ∈ N), ; ! | |
	∪(𝒞') = X ; subset
),
[ equivalently``` ; +
	∀X: ∈ compact_set,
	∀ℱ: ⊆ {∀x: ∈ X.closed},
	; ∀S: ∈ finite(ℱ)
	```
	proof``` ; +
	∀C: ∈ compact_set,
	∀𝒞: ⊆ set.cover(C),
``` ]
