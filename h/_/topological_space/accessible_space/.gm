: ; accessible_space, or T1_space
(
	⊆ topological_space,
	(
		∀H: ∈ space,
		; the open set version
		∀{x, y}: ∈ H,
		!(x = y) ⇒
		(
			∃X: ∈ X.nbh(x), !(y ∈ X)
		)
	)
),
[ chaim: discrete_space ⊆ Hausdroff_space ⊆ accessible_space ⊆ Kolmogorov_space ]
[ property:
	+ every singleton are closed, vice verse
	+ example: infinite space with cofinite topology
 ]
