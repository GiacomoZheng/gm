: ; homomorphism
(
	; ⊆ set.homomorphism, ; mapping
	∀f: ∈ homomorphism,
	@f,
		D ∈ topological_space, C ∈ topological_space,
		∀U: ⊆ C,
		U ∈ C.τ ⇔ inv_image(U) ∈ D.τ ; ? inverse image
	■
),
[ equivalently```
	f ∈ continuous_function,
	inv_image(U) ∈ D.τ ⇒ U ∈ C.τ
``` ]
