: ; Kolmogorov_space, or T0_space
(
	⊆ topological_space,
	(
		∀K: ∈ Kolmogorov_space,
		∀{x, y}: ⊆ K,
		¬(x = y) ⇒
		∃(
			; open set is the same, compare it with T1, T2
			∃X: ∈ X.nbh(x), ¬(y ∈ X),
			∃Y: ∈ H.nbh(y), ¬(x ∈ Y)
		)
	)
),
[ chiam: Kolmogorov_space ⊇ accessible_space ⊇ Hausdorff_space ]
