: ; + compact_space
(
	⊆ topological_space,
	∀𝒞: ∈ open_cover(X),
	∃𝒞': (⊆ 𝒞, |𝒞'| ∈ N), ; ! | |
	∪(𝒞') = X ; subset
),
[ equivalently``` ; +
	∀X: ∈ compact_space,
	∀ℱ: ⊆ {∀x: ∈ X.closed},
	; ∀S: ∈ finite(ℱ)
	```
	proof``` ; +
	∀C: ∈ compact_space,
	∀𝒞: ⊆ cover(C),
``` ]
