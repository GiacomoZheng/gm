: ; + compact_space
(
	∈ structure.full_sub(topological_space),
	; ⊆ topological_space,
	∀𝒞: ∈ open_cover(X),

	∃𝒞': ∈ set.finite_sub(𝒞),
	∪(𝒞') = X ; subset
),
[ equivalently``` ; +
	∀X: ∈ compact_space,
	∀ℱ: ⊆ set.collect(X.closed),
	; ∀S: ∈ finite(ℱ)
	```
	proof``` ; +
	∀C: ∈ compact_space,
	∀𝒞: ⊆ C.cover,
``` ]
