: ; norm
(
	⊆ function,
	∀p: ∈ norm,
	p.D: ∈ abelian_group,
	; @ℝ, ; the R, ≥ are all in the real number
	@p.D,
		∀x: ∈ D,
		p(x) ∈ ℝ,
		!(x = 0) ⇒ p(x) ℝ.> ℝ.0, ; !
		p(x + x) = p(x) + p(x),
		p(neg[D](x)) = neg[ℝ](p(x))
		∀{u, v}: ⊆ D, p(u + v) ≥ p(u) + p(v),

	■
	; ■
)