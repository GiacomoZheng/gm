; all the theorems about coset are in group
s"all the subgroups are normal":
(
	∀G: ∈ abelian_group,
	normal(G) class.= sub(G)
),

s"classification of finite abelian group": ; +
(
	∀T: ∈ abelian_group, n: = set.#(T),
	T ∈ finite_group ⇒
),

s"Fundamental Theorem of Finite generated abelian group":
(
	∀G: ∈ finitely_generated_group,
	G ∈ abelian_group ⇒
	(
		∃m: ∈ ℕ, (∃T: ∈ abelian_group, set.#(T) ∈ ℕ), G ≌ ℤ^m × T ; !
	)
),
; Proof
