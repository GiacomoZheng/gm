; + 
"power set", "poset", "maximal",
; Theorem: start
	∀X: ∈ set, Ps(X) ∈ poset, &PS(X).≤& = &⊆&,
	X ∈ maximal_ideal(G) ; in fact, it is the only one
; Theorem: end

"finite set", "posoet", "maximal",
; Theorem: start
	∀P: ∈ poset,
	|P| ∈ ℕ ⇒ (∃x: ∈ P, x ∈ maximal_ideal(G)),
; Theorem: end

