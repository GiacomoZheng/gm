: ; cyclic_group
(
	⊆ group,

	(
		∀C: ∈ cyclic_group,
		∃g: ∈ C, ; no need to exclude identity, and it may not be only one
		C = {C.e (C.* g)^n}[∀n: ∈ ℤ], ; !
	),

	; location
		generator(∀G: ∈ cyclic_group): ; !
		(
			⊆ set.element(G),
			∀g: ∈ generator(G),
			G = {G.e (G.* g)^n}[∀n: ∈ ℤ] ; !
		),
),
; we call it "the group generated by g"
[ hierarchy chain: cyclic_group ⊆ abelian_group ]