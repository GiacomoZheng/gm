: ; homomorphism
(
	⊆ set.homomorphism, ; mapping
	(
		∀f: ∈ homomorphism,
		f.D ∈ group, f.C ∈ group
	)
),
[ ```
	∀a: ∈ G,
	@f,
	C.e = f(a) C.* inv(f(a)) = f(D.e D.* a) C.* inv(f(a)) = f(D.e) C.* f(a) C.* inv(f(a)) = f(D.e),
	e = f(e) = f(a * inv(a)) = f(a) * f(inv(a)) ⇒ f(inv(a)) = inv(f(a))
	■
``` ]
