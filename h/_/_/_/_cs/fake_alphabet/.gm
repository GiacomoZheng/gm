: ; _alphabet
(
	"a monoid of strings, in fact, it is a free monoid",
	⊆ monoid,
	(
		∀A: ∈ alphabet,
		∀α: ∈ A, α ∈ string, ; ? string
		A.*: = &string.+&, ; ? +
		A.e: = "",
		[ it means that if "a", "b" are in A, "ab" is also in A ]
	)
),