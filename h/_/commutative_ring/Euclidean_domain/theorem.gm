[ chaim: commutative_ring ⊆ inetral_domain ⊆ GCD_domain ⊆ UFD ⊆ PID ⊆ ED ]
s"Bézout's identity":
(
	∀R: ∈ ED,
	@R,
	∀{a, b}: ⊆ R\{0},
	(∃{u, v}: ⊆ S, a × u + b × u = gcd(a, b))
	; (∀{u, v}: ⊆ S, a × u + b × u / gcd(a, b) ∈ S)
	■
),

