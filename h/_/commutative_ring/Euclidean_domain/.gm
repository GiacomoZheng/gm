: ; + Euclidean_domain, or ED, I'll define it after I learn something about lattice
(
	"a integral domain (ring) satisfies the Euclidean algorithm",
	⊆ integral_domain, ; here it needn't be unital, and we can prove it is.
	(
		∀R: ∈ Euclidean_domain,
		R.σ

	),

	; location
		; about inheritance
			divisor[∀E: ∈ ED]: = UFD.divisor[E],

			multiple[∀E: ∈ ED]: = UFD.multiple[E],

			associated[∀E: ∈ ED]: = UFD.associated[E],

			common_divisor[∀E: ∈ ED]: = UFD.common_divisor[E],

			gcd[∀E: ∈ ED]: = UFD.common_divisor[E],
		; end

		Euclidean_function: ; + 
		(
			"from an integral domain to a well ordered set",
			⊆ mapping,
			(
				∀f: ∈ Euclidean_function,
				f.D ∈ integral_domain,
				f.C ∈ well_ordered_set, ; + define it later
				@f, ; ?
				f(D.0) = minimal(D) ; ! I don't know now, I'll try to prove it
				■
			)
		),


),
[ chaim: ED ⊆ PID ⊆ UFD ⊆ integral_domain (ring) ⊆ commutative_ring ]