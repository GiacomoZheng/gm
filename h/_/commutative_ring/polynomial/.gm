: ; polynomial
(
	⊆ function,
	(
		∀f: ∈ polynomial,
		f.R: ∈ commutative_ring,
		∀R': (∈ commutative_ring, R ∈ commutative_ring.sub(R')),
		∃n: ∈ h.N, S: = {m|h.N.0 ≤ m < n}[∀m: ∈ h.N],
		f.c[∀m: ∈ S]: ∈ R,
		@R,
			∀x: ∈ R', f(x) = +[∀m: ∈ S](f.c[m] × (x ^ m))
		■
	),
	; location
		deg(∀f: ∈ polynomial): ; +
		(
			"the power of maximal nonzero item",

		),

		root(∀f: ∈ polynomial):
		(
			⊆ set.element(f.R),
			∀α: ∈ root(f),
			f(α) = R.0
		),
),
