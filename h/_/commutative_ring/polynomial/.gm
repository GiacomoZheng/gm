: ; polynomial
(
	⊆ function,
	(
		∀f: ∈ polynomial,
		f.R: ∈ commutative_ring,
		@ℕ,
			∃n: ∈ ℕ, S: = {m|0 ≤ m < n}[∀m: ∈ ℕ],
			f.c[∀m: ∈ S]: ∈ R
		■
		@R,
			∀x: ∈ ◉,
			∃R': (∈ commutative_ring, R ∈ commutative_ring.sub(R'), x ∈ R'), ; ?
			f(x) = +[∀m: ∈ S](f.c[m] × (x ^ m))
		■
		; attributes
			f.trivial(∀x: ∈ ◉): = f.R.0 ; ?
	),
	; location
		deg(∀f: ∈ polynomial): ; +
		(
			"the power of maximal nonzero item",

		),

		root(∀f: ∈ polynomial):
		(
			⊆ set.element(f.R),
			∀α: ∈ root(f),
			f(α) = R.0
		),
),
