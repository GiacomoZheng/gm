: ; homomorphism
(
	⊆ set.homomorphism, ; mapping
	(
		∀f: ∈ homomorphism,
		; f.D ∈ magma, f.C ∈ magma, ; from structure.sub(set)
		∀{x, y}: ∈ f.D,
		@f,
			f( D.operation(x)(y)) = C.operation(f(x)) (f(y))
		■
	)
),