: ; UFD, unique_factorization_domain
; !
(
	"the integal domain in which every non-zero element not a unit can be written as the product of finite unique irreducible factorization",
	∈ structure.full_sub(integral_domain),
	... ; +
	∀U: ∈ UFD,
	[ hierarchy chain: integral_domain ⊇ GCD_domain ⊇ UFD ]
	; location
		; about inheritance
		; end
),
[ hierarchy chain: UFD ⊆ integral_domain (ring) ⊆ commutative_ring ]