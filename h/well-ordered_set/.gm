: ; + well-ordered_set, or well_ordered_set, or least_well_ordered_set
(
	⊆ totally-ordered_set,
	"every subset of it has its least",
	(
		∀S: ∈ well-ordered_set,
		∀R: ∈ sub(S), ∃x: ∈ S, x ∈ R.least
		; attributes
			S.initial_segment(∀a: ∈ S): = {x|x S.< a}[∀x: ∈ S],
				S.seg: = S.initial_segment,
	),

	; location
		; about inheritance	
			sub(∀R: ∈ well-ordered_set): = poset.sub(R), ; ?
			; * every subset of a well-ordered set is still a well-ordered set
		; end
),