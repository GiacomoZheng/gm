: ; + string
(
	⊆ ◉,
	(

	),
	; location
		(
			∀α: ∈ string, ∀β: ∈ string,
			+:
			(
				; + tag: delimiter
				∈ keyword,
				(α + β) = ... ; +
			)
		),
		length(∀x: ∈ string): = ; +
			len: = length, ; ?
),