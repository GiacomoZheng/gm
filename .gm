[…]: …, ; annotation
[ 
	the definition of these
	' symbols '
	@		--- locator
	■		--- terminator
	 		---	delimiter
	( )		---	bracket
	∀ ∃ !	---	quantifier
	.		---	operator
	⇒		---	relator
	:		---	definer
	,		---	separator ⊆ operator
	' others '
	" "
	◉
 ]

[ 
	priority:
	locator terminator
	none bracket function
	quantifier relator
	operator
	definer
	separator  
 ]

[ ```
⇒:
(
	∈ relator, ; a sample of symbol
	∀A: ∈ statement,
	∀B: ∈ statement,
	B ⇒ (A ⇒ B),
	(∀C: ∈ statement, A ⇒ (B ⇒ C)) ⇒ ((A ⇒ B) ⇒ (A ⇒ C)),
)
A ⇒ (B ⇒ A),
(!(A) ⇒ !(B)) ⇒ (B ⇒ A),

!(A) ⇒ (A ⇒ B),



∀a: ∈ ◉,
A: = {a, {a}},
B: = {a},
∀(B ∈ A, B ⊆ A),

∃f: ∈ permutation,
; the mapping with the same domain and codomain
f.D = A,
f(a) = {a},
f({a}) = a,

[ now, what is f(B)? ]
f(B) = f({a}) = a, ; as element,
f(B) = {f(a)} = {{a}}, ; as subset,

``` ]